# Working Assets

Store in-progress reusable asset iterations here. Use clear status and avoid presenting these as approved. Remove obsolete working files only with normal project cleanup approval and version-control safety.
